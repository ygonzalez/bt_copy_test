<% @title = "Showing trade" %>

<div class="form-horizontal">
  <div class="control-group">
    <label class="control-label">Name</label>
    <div class="controls">
      <span class="uneditable-input span6"><%= @trade.name %></span>
    </div>
  </div>
  
  <div class="form-actions">
    <%= link_to icon_tag("pencil") + ' Edit', edit_trade_path(@trade), class: 'btn btn-primary' %>
    <%= link_to 'Back', trades_path, class: 'btn' %>
  </div>
</div>

