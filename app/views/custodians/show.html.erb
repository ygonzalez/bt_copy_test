<% @title = "Showing custodian" %>

<div class="form-horizontal">
  <div class="control-group">
    <label class="control-label">Name</label>
    <div class="controls">
      <span class="uneditable-input span6"><%= @custodian.name %></span>
    </div>
  </div>
  
  <div class="form-actions">
    <%= link_to icon_tag("pencil") + ' Edit', edit_custodian_path(@custodian), class: 'btn btn-primary' %>
    <%= link_to 'Back', custodians_path, class: 'btn' %>
  </div>
</div>

