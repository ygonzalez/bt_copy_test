<% @title = "Showing security" %>

<div class="form-horizontal">
  <div class="control-group">
    <label class="control-label">Name</label>
    <div class="controls">
      <span class="uneditable-input span6"><%= @security.name %></span>
    </div>
  </div>
  
  <div class="form-actions">
    <%= link_to icon_tag("pencil") + ' Edit', edit_security_path(@security), class: 'btn btn-primary' %>
    <%= link_to 'Back', securities_path, class: 'btn' %>
  </div>
</div>

