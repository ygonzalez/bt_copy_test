<% @title = "Listing securities" %>

<% content_for :description do %>
  <p>Edit this template in <code>app/views/Securities/index.html.erb</code>.</p>
<% end %>

<% content_for :actions do %>
  <%= link_to new_security_path, class: 'btn btn-primary btn-large pull-right' do %>
    <%= icon_tag "plus" %> Add Security
  <% end %>
<% end %>

<div class="sheet sheet-condensed">
  <div class="sheet-inner">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% @securities.each do |security| %>
        <tr>
          <td><%= security.name %></td>
          <td class="actions">
            <%= link_to icon_tag("eye-open"), security, rel: 'tooltip', title: 'Show' %>
            <%= link_to icon_tag("pencil"), edit_security_path(security), rel: 'tooltip', title: 'Edit' %>
            <%= link_to icon_tag("trash"), security, rel: 'tooltip', title: 'Delete',
                        data: { confirm: 'Are you sure?' }, method: :delete %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div><!-- /.sheet-inner -->
</div><!-- /.sheet -->

<%# Remove the trailing if condition if you're using pagination. %>
<%= will_paginate @securities if @securities.respond_to?(:total_pages) %>
